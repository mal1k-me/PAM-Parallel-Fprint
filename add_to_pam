# Add this to the top of /etc/pam.d/your-service (e.g., /etc/pam.d/polkit-1 or /etc/pam.d/sudo)
# This allows parallel fingerprint and password authentication
auth    [success=done default=ignore]   pam_parallel_fprint.so
auth    required    pam_unix.so try_first_pass
